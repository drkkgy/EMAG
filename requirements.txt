# Core dependencies for the EMAG SD3 path.
# Tested with the pinned versions below; newer diffusers may change the SD3 pipeline API.
torch==2.3.1
diffusers==0.31.0
transformers>=4.41.0,<4.49  # newer transformers require torch>=2.5, which conflicts with torch==2.3.1
accelerate>=0.30.0
safetensors>=0.4.0
sentencepiece>=0.1.99   # required by the SD3 T5 tokenizer
protobuf<5
Pillow
numpy
# DiT (class-conditional) path
timm>=0.9.0
torchvision
tqdm
# Evaluation (Table 2 reproduce: FID + HPS)
pytorch-fid
hpsv2
